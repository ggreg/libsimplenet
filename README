libsimplenet is a simple network library on top of libev.

At the moment, it is written and tested on linux amd64. This first version is
more a example that a usable library. The next version will allow to define
custom callback for request processing and logging.
